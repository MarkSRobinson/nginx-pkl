amends "chart.pkl"

config {
  services {
    ["nginx"] {
      spec {
        type = "ClusterIP"
        ports {
          new {
            port = 80
            targetPort = "http"
            protocol = "TCP"
            name = "http"
          }
        }
      }
    }
  }
}